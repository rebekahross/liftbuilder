{
  "name": "liftbuilder-backend",
  "version": "1.0.0",
  "description": "Backend API for LiftBuilder workout app",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.x.x",
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "dotenv": "^16.x.x",
    "express": "^4.x.x"
  },
  "devDependencies": {
    "nodemon": "^2.x.x"
  }
}
